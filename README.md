# miller-pb

A fresh project. Nothing here yet — built from scratch.

## Setup

_To be added as the project takes shape._

## Backend

This project connects to its own Supabase project (created and owned by me).
Environment variables live in `.env` (gitignored) — see `.env.example` once it exists.
