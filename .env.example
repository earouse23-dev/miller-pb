# Supabase — client-side (safe to expose; this is the publishable/anon key).
# Copy to .env locally, and set the same vars in your host (e.g. Vercel) env.
VITE_SUPABASE_URL="https://YOUR-PROJECT.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="your-publishable-anon-key"
